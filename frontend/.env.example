# Base URL of the NestJS backend, including the /api prefix.
VITE_API_BASE=http://localhost:3000/api

# Required header for /api/university/* routes (set in backend .env as ADMIN_API_KEY).
# Leave blank if you only want to ping non-admin routes.
VITE_ADMIN_API_KEY=
